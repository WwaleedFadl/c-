#include <iostream>
#include <vector> 
#include <iomanip>
using namespace std; 

int main(){
  /*
  int scores [] {10,20,30};
  for(auto score :scores){
    cout << score << endl;
  }
  vector<double> tempreatures{44.4,22.2};
  double average_temp{};
  double total{};

  for(auto temp:tempreatures)
    total += temp; 

  cout << total << endl;

  if(tempreatures.size() != 0) 
    average_temp = total / tempreatures.size();

  cout << fixed << setprecision(1);

  cout << average_temp << endl;

  for(auto val:{1,2,3,4,5})
    cout << val << endl;

  for(auto c:"Wa leeeeeeeeeeeeeeeeeeeeeed")
    if(c != ' ')
      cout << c;
  for(auto c:"Wa leeee  eeeee  eee     eeeee    eeeeed")
      if(c == ' ') 
        cout << '\t';
      else
      cout << c;
  */
  cout << endl;
  return 0;
}
